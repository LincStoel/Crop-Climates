modLoader = "javafml"
loaderVersion = "[4,)"
license = "MIT"

[[mods]]
modId = "crop_climates_stress"
version = "0.0.0"
displayName = "Crop Climates Stress Harness"
description = '''
Dev-only harness for stress testing crop_climates: forced chunk ticking,
scenario builders, per-tick timing, growth tracking and diagnostics counters.
Loaded only by the stressServer/stressClient runs; never shipped.
'''

[[mixins]]
config = "crop_climates_stress.mixins.json"

[[dependencies.crop_climates_stress]]
modId = "crop_climates"
type = "required"
versionRange = "[0,)"
ordering = "AFTER"
side = "BOTH"
